function failed(mes){ console.error(mes); process.exit(1); } const input = process.argv[2]; if(!input){ failed("invalid input"); } const re = /^(\d{4})-(\d{2})-(\d{2})$/; const m = re.exec(input); if(!m){ failed("invalid format"); } const year = Number(m[1]); const month = Number(m[2]) - 1; const day = Number(m[3]); const inputDate = new Date(year, month, day); if (!(inputDate.getFullYear() === year && inputDate.getMonth() === month && inputDate.getDate() === day)){ failed('uncorrect format'); } const today = new Date(); const birthUTC = Date.UTC(year, month, day); const limitUTC = Date.UTC( today.getFullYear()-14, today.getMonth(), today.getDate() ); console.log(birthUTC < limitUTC)
    